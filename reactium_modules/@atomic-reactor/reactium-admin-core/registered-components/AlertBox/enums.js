import { __ } from '@atomic-reactor/reactium-core/sdk';

const ENUMS = {
    TEXT: {
        LABEL: {
            OK: __('Ok'),
        },
        TITLE: __('Alert'),
    },
};

export default ENUMS;
