import { __ } from '@atomic-reactor/reactium-core/sdk';

const ENUMS = {
    TEXT: {
        LABEL: {
            NO: __('No'),
            YES: __('Yes'),
        },
        MESSAGE: __('Are you sure?'),
        TITLE: __('Confirm'),
    },
};

export default ENUMS;
