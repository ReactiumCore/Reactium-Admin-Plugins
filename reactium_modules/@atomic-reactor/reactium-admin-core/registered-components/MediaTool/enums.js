import { __ } from 'reactium-core/sdk';

const ENUMS = {
    defaultPickerOptions: {
        maxSelect: 1,
        filters: ['all'],
        title: __('Select Media'),
    },
};

export default ENUMS;
