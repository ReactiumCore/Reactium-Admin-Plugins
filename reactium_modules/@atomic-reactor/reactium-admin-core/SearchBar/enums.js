import { __ } from 'reactium-core/sdk';

const ENUMS = {
    TEXT: {
        ARIA_CLEAR: __('clear search'),
        ARIA_SEARCH: __('Search'),
        PLACEHOLDER: __('Search'),
    },
};

export default ENUMS;
