import React from 'react';
import cn from 'classnames';

const ContentType = props => {
    console.log(props);
    return <div className={cn('media-editor')}>Content Type Editor</div>;
};

export default ContentType;
