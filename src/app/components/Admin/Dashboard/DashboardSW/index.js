import React from 'react';
import MenuItem from 'components/Admin/MenuItem';

export default props => (
    <MenuItem
        label='Dashboard'
        route='/admin'
        icon='Linear.Window'
        count={5000}
    />
);
