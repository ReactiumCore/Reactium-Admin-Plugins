import { __ } from 'reactium-core/sdk';

const ENUMS = {
    TEXT: {
        EDITOR: __('Editor'),
        LIST: __('Content'),
        NEW: __('New'),
        SAVE: __('Save %type'),
        SAVING: __('Saving %type...'),
    },
};

export default ENUMS;
