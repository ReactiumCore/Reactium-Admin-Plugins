import { __ } from 'reactium-core/sdk';

const ENUMS = {
    TEXT: {
        EDITOR: __('Editor'),
        LIST: __('Content'),
        NEW: __('New'),
        SAVE: __('Save'),
    },
};

export default ENUMS;
